{
  "tool": "OpenSpecWriter@v1",
  "args": {
    "slug": "example-openspec-test",
    "title": "Example: confirm Trusted Tool write",
    "rationale": "Smoke test payload generated during scaffolding.",
    "acceptance_criteria": [
      "File created under openspec/changes",
      "Digest updated via npm run context:openspec"
    ],
    "notes": [
      "This payload is safe to rerun; it overwrites the same slug."
    ]
  }
}

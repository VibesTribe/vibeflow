name: Require Human Approval
on:
  pull_request:
    branches: [ "main" ]

jobs:
  require-approval:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://github.com/VibesTribe/vibeflow
    steps:
      - name: Approval Required
        run: echo "Manual approval required before merging to main."

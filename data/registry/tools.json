{
  "$schema": "https://vibeflow.dev/schemas/tool.registry.json",
  "updated_at": "2025-10-11T18:01:00Z",
  "tools": [
    {
      "slug": "OpenSpecWriter@v1",
      "description": "Write OpenSpec change proposals from structured args",
      "entrypoint": "scripts/tools/dispatch-trusted-tool.mjs",
      "allowed_args": ["slug", "title", "rationale", "acceptance_criteria", "notes"],
      "tags": ["trusted", "docs"],
      "outputs": ["openspec/changes/<slug>.md"],
      "policy_flags": ["PII_FREE"]
    },
    {
      "slug": "VisualChecklist@v1",
      "description": "Persist visual audit checklist output",
      "entrypoint": "scripts/tools/dispatch-trusted-tool.mjs",
      "allowed_args": ["task_id", "title", "summary", "steps", "status"],
      "tags": ["trusted", "visual"],
      "outputs": ["docs/visual/checklists/<task_id>.md"],
      "policy_flags": ["HUMAN_APPROVAL_REQUIRED"]
    }
  ]
}
